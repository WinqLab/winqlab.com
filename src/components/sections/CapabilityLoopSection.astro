---
import type { Capability } from "../../data/capabilities";

interface Props {
  items: Capability[];
}

const { items } = Astro.props;
---

<section class="section-scene section-scene--dark">
  <div class="section-inner capability-loop">
    <div class="section-lede section-lede--center section-lede--wide">
      <p class="eyebrow">System Loop</p>
      <h2>五个能力。一个闭环。</h2>
      <p>
        从可信输入到持续反馈，每一步都服务于更稳健的研究判断，并以持续获取超额收益为目标。
      </p>
    </div>

    <div class="loop-line" aria-label="WinQ Lab 核心能力闭环">
      {
        items.map((item) => (
          <a class="loop-node" href={`/${item.slug}`}>
            <span class="loop-node__order">{item.order}</span>
            <strong>{item.title}</strong>
            <span>{item.subtitle}</span>
          </a>
        ))
      }
    </div>
  </div>
</section>
